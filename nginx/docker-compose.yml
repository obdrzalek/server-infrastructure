name: nginx-cerbot
services:
  nginx:
    image: nginx:stable-alpine
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ${HOME}/certbot/etc:/etc/letsencrypt:ro
      - ${HOME}/certbot/var:/var/lib/letsencrypt:ro
      - ${HOME}/certbot/webroot:/var/www/html
    networks:
      - server

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - ${HOME}/certbot/etc:/etc/letsencrypt
      - ${HOME}/certbot/var:/var/lib/letsencrypt
      - ${HOME}/certbot/webroot:/var/www/html
    # Certbot se po spuštění a ověření sám vypne, neběží pořád
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - server

networks:
  server:
    external: true
    name: server
